service:
  name: hr-leave-service
  port: 80

image:
  repository: ghcr.io/company/hr-leave-service
  tag: "1.0.0"

replicaCount: 2
containerPort: 8080

ingress:
  enabled: true
  className: nginx
  host: leave.hr.example.com
  path: /

serviceMonitor:
  enabled: true
